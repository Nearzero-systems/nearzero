/** @deprecated Use backendProxy — kept for copied Nearzero API routes. */
export {
	getSession,
	proxyBackendAuth as proxyControlPlaneAuth,
	proxyBackendRequest as proxyControlPlaneRequest,
} from "./backendProxy";
