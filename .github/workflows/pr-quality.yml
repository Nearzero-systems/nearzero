
name: PR Quality

permissions:
  contents: read
  issues: read
  pull-requests: write

on:
  pull_request_target:
    types: [opened, reopened]

jobs:
  anti-slop:
    runs-on: ubuntu-latest
    steps:
      - uses: peakoss/anti-slop@753c51043d0c43564e1df382c173233c56cdb5e4 # v0
        with:
          blocked-commit-authors: "claude,copilot"
          require-description: true
          min-account-age: 5
